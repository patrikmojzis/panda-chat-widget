{
  "name": "@panda-chat-widget/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"TODO: dev command not implemented yet for @panda-chat-widget/server\"",
    "build": "echo \"TODO: build command not implemented yet for @panda-chat-widget/server\"",
    "test": "echo \"TODO: tests not implemented yet for @panda-chat-widget/server\"",
    "typecheck": "sh -c 'if find src -type f \\( -name \"*.ts\" -o -name \"*.tsx\" \\) 2>/dev/null | grep -q .; then tsc -p tsconfig.json --noEmit --pretty false; else echo \"TODO: add server TypeScript sources\"; fi'",
    "lint": "echo \"TODO: lint command not implemented yet for @panda-chat-widget/server\"",
    "check": "pnpm typecheck && pnpm lint && pnpm test && pnpm build"
  }
}
