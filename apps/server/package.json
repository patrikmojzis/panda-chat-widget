{
  "name": "@panda-chat-widget/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "sh -c 'if [ -f src/main.ts ]; then node --watch src/main.ts; else echo \"TODO: add server src/main.ts entrypoint\"; fi'",
    "build": "echo \"TODO: build command not implemented yet for server API/DB/SSE service\"",
    "test": "echo \"TODO: tests not implemented yet for server API/DB/SSE service\"",
    "typecheck": "sh -c 'if find src -type f \\( -name \"*.ts\" -o -name \"*.tsx\" \\) 2>/dev/null | grep -q .; then tsc -p tsconfig.json --noEmit --pretty false; else echo \"TODO: add server TypeScript sources\"; fi'",
    "lint": "echo \"TODO: lint command not implemented yet for server API/DB/SSE service\"",
    "check": "pnpm typecheck && pnpm lint && pnpm test && pnpm build"
  },
  "description": "Owns the future API, database, and SSE service for the chat widget.",
  "dependencies": {
    "fastify": "^5.8.5"
  },
  "type": "module"
}
