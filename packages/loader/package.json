{
  "name": "@panda-chat-widget/loader",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"TODO: dev command not implemented yet for @panda-chat-widget/loader\"",
    "build": "echo \"TODO: build command not implemented yet for @panda-chat-widget/loader\"",
    "test": "echo \"TODO: tests not implemented yet for @panda-chat-widget/loader\"",
    "typecheck": "sh -c 'if find src -type f \\( -name \"*.ts\" -o -name \"*.tsx\" \\) 2>/dev/null | grep -q .; then tsc -p tsconfig.json --noEmit --pretty false; else echo \"TODO: add loader TypeScript sources\"; fi'",
    "lint": "echo \"TODO: lint command not implemented yet for @panda-chat-widget/loader\"",
    "check": "pnpm typecheck && pnpm lint && pnpm test && pnpm build"
  }
}
