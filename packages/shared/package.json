{
  "name": "@panda-chat-widget/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json --pretty false",
    "test": "node --test \"test/**/*.test.mjs\"",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "check": "pnpm typecheck && pnpm build && pnpm test"
  },
  "description": "Shared visitor identity contract.",
  "main": "./dist/visitor-identity.js",
  "types": "./dist/visitor-identity.d.ts",
  "exports": {
    ".": {
      "types": "./dist/visitor-identity.d.ts",
      "import": "./dist/visitor-identity.js"
    }
  },
  "files": [
    "dist"
  ]
}
